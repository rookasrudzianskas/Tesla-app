import React from 'react';
import {FlatList, Text, View} from "react-native";

const CarsList = () => {
    return (
        <View>
            <FlatList data={}  showsVerticalScrollIndicator={false} showsHorizontalScrollIndicator={false}/>
        </View>
    );
};

export default CarsList;
